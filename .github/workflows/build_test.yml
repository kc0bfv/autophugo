name: Build Test
on:
    workflow_dispatch:
jobs:
    build-test:
        runs-on: ubuntu-latest
        steps:
            - name: Install Hugo
              run: sudo snap install hugo
            - name: Checkout Repo
              uses: actions/checkout@v2
            - name: Build Pages
              run: make
